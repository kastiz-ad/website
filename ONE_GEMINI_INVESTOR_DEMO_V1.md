# ONE Gemini Investor Demo V1

`GEMINI_DEMO` is founder-only, explicitly selected, synthetic-data-only and never automatic. The founder indicator is `Investor Demo — Gemini`. Missing credentials, enablement or privacy review produce a deterministic DEMO fallback. Ordinary customer results never expose provider selection.
