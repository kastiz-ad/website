export const MODEL_TIERS=Object.freeze(["LIGHTWEIGHT","STANDARD","ADVANCED_REASONING","SAFETY_REVIEW"]);
export function modelPolicy(env={}){return Object.freeze({LIGHTWEIGHT:env.OPENAI_MODEL_LIGHTWEIGHT||"gpt-5.6-luna",STANDARD:env.OPENAI_MODEL_STANDARD||env.OPENAI_MODEL||"gpt-5.6-terra",ADVANCED_REASONING:env.OPENAI_MODEL_ADVANCED||"gpt-5.6-sol",SAFETY_REVIEW:env.OPENAI_MODEL_SAFETY||"gpt-5.6-sol"});}
export function selectModelTier({task="",tasks=0,providers=0,highRisk=false,conflictingConstraints=false}={}){const text=String(task).toLowerCase();if(highRisk||/(medical|health|legal|financial|passport|visa|identity|payment|government|의료|법률|금융|여권|결제)/i.test(text))return"SAFETY_REVIEW";if(conflictingConstraints||tasks>=8||providers>=4||/(complex|multi-person|relocation|business operations|technical|failure recovery)/i.test(text))return"ADVANCED_REASONING";if(tasks<=1&&providers===0&&/(detect|classif|rewrite|short|ui text|language)/i.test(text))return"LIGHTWEIGHT";return"STANDARD";}
export function resolveModel(input,env={}){const tier=selectModelTier(input);return{tier,model:modelPolicy(env)[tier],reasoningEffort:tier==="ADVANCED_REASONING"||tier==="SAFETY_REVIEW"?"high":tier==="STANDARD"?"medium":"low"};}
