import{ONE_AGENT_INSTRUCTIONS}from"./one-agent-instructions.js";import{agentConfig}from"./one-agent-config.js";import{validateAgentOutput}from"./one-agent-validator.js";
import{resolveModel}from"./one-model-policy.js";import{pricingMetadata,estimateAiCost,assertCostPermission}from"./one-cost-governance.js";
export async function runOpenAIAgent({env,context,draft,requestKey}){const cfg=agentConfig(env);if(!cfg.enabled||!cfg.apiKey)return{mode:"DEMO",mission:draft,reason:"OpenAI agent is disabled or not configured."};const route=resolveModel({task:draft.userRequest,tasks:draft.tasks?.length,providers:draft.toolsRequired?.length,highRisk:draft.risk?.level==="HIGH"},env),pricing=pricingMetadata(env,route.model),estimatedInput=Math.ceil(JSON.stringify({context,draft}).length/4),estimatedOutput=cfg.maxOutputTokens,estimatedUsd=estimateAiCost({inputTokens:estimatedInput,outputTokens:estimatedOutput,pricing});const cost=assertCostPermission({accountId:context.accountId,workspaceId:context.workspaceId,requestKey,estimatedUsd,env});const OpenAI=(await import("openai")).default,client=new OpenAI({apiKey:cfg.apiKey,project:cfg.projectId||undefined,timeout:cfg.timeoutMs,maxRetries:cfg.maxRetries});const response=await client.responses.create({model:route.model,instructions:ONE_AGENT_INSTRUCTIONS,input:JSON.stringify({context,draft}),reasoning:{effort:route.reasoningEffort},max_output_tokens:cfg.maxOutputTokens,text:{format:{type:"json_object"}}});const parsed=JSON.parse(response.output_text);return{mode:"LIVE",mission:validateAgentOutput(parsed),responseId:response.id,modelTier:route.tier,cost:{...cost,pricingStatus:pricing.status}};}
