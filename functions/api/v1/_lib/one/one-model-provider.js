export const INTELLIGENCE_PROVIDERS=Object.freeze(["OPENAI","GEMINI","DEEPSEEK","DEMO"]);
export function modelProviderConfig(env={}){return Object.freeze({defaultProvider:"OPENAI",OPENAI:{configured:Boolean(env.OPENAI_API_KEY),enabled:env.OPENAI_AGENT_ENABLED==="true",privacyReviewed:true},GEMINI:{configured:Boolean(env.GEMINI_API_KEY),enabled:env.GEMINI_ENABLED==="true",privacyReviewed:env.GEMINI_PRIVACY_REVIEWED==="true"},DEEPSEEK:{configured:Boolean(env.DEEPSEEK_API_KEY),enabled:env.DEEPSEEK_ENABLED==="true",privacyReviewed:env.DEEPSEEK_PRIVACY_REVIEWED==="true"},DEMO:{configured:true,enabled:true,privacyReviewed:true}});}
export function selectIntelligenceProvider(requested,env={}){const config=modelProviderConfig(env),provider=requested||config.defaultProvider;if(!INTELLIGENCE_PROVIDERS.includes(provider))throw new TypeError("unsupported_intelligence_provider");const state=config[provider];if(provider!=="OPENAI"&&provider!=="DEMO"&&(!state.enabled||!state.configured||!state.privacyReviewed))return{provider:"DEMO",reason:`${provider} is not founder-configured and privacy-reviewed`,silentSwitch:false};return{provider,reason:null,silentSwitch:false};}
