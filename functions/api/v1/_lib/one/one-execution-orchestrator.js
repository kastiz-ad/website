import{canExecute}from"./one-agent-safety.js";
export class ExecutionOrchestrator{constructor({maxRetries=1}={}){this.maxRetries=maxRetries;}run(actions=[],approvals=[]){const receipts=[],queue=[...actions].sort((a,b)=>(a.order||0)-(b.order||0));for(const action of queue){const approval=approvals.find(item=>item.approvalId===action.approvalId);if(!canExecute(action,approval)){receipts.push({actionId:action.actionId,status:"BLOCKED",reason:"Approval or production provider unavailable",executed:false});continue;}receipts.push({actionId:action.actionId,status:"SIMULATED",executed:false,attempts:1});}return{status:receipts.every(x=>x.status==="SIMULATED")?"READY_TO_COMPLETE":"NEEDS_ATTENTION",receipts,externalExecution:false};}}
