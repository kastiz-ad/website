{
  "name": "kastiz-one-secure-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check functions/api/v1/[[path]].js && node --check functions/_middleware.js",
    "security:scan": "node tools/security-scan.mjs"
  }
}
