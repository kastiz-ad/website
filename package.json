{
  "name": "kastiz-one-secure-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node tools/static-quality-check.mjs && node --check functions/api/v1/[[path]].js && node --check functions/api/v1/one-pass/[[path]].js && node --check functions/_middleware.js",
    "security:scan": "node tools/security-scan.mjs",
    "ci": "npm run check && npm run security:scan && npm test"
  },
  "dependencies": {
    "@simplewebauthn/server": "13.3.2",
    "openai": "^5.10.0"
  }
}
