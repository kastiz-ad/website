const block=(day,time,type,title,{duration=90,area=null,weatherDependent=false,reservation="NOT_REQUIRED",estimatedCost=null,backup=null}={})=>({day,time,type,title,durationMinutes:duration,area,weatherDependent,reservationStatus:reservation,estimatedCost,backup,sourceStatus:"DEMO_OR_ESTIMATED"});
const experienceRotation=["Signature landmark and orientation","Neighborhood and local culture","Museum or indoor cultural experience","Park, nature, and photography","Local market and food experience","Flexible priority experience"];
export function planBalancedExperience(context={}){const days=Math.max(1,Number(context.durationDays)||1),profile=context.pacingProfile||"BALANCED",destination=context.destination||"Destination",timeline=[];for(let day=1;day<=days;day++){const focus=experienceRotation[(day-1)%experienceRotation.length];timeline.push(block(day,"09:30","main-attraction",`${destination}: ${focus}`,{duration:profile==="RELAXED"?120:150,weatherDependent:focus.includes("landmark")||focus.includes("Park"),backup:"Verified indoor alternative required"}));timeline.push(block(day,"12:30","meal","Local meal and recovery",{duration:75}));timeline.push(block(day,"14:00","rest","Flexible or rest time",{duration:profile==="ACTIVE"?45:90}));if(day<days||profile!=="RELAXED")timeline.push(block(day,"16:00","secondary-experience",`${focus} - nearby secondary option`,{duration:120}));timeline.push(block(day,"19:00","evening","Dinner or optional evening experience",{duration:120,reservation:"VERIFY_IF_SELECTED"}));timeline.push(block(day,"21:30","safe-return","Safe return plan",{duration:45}));}return timeline;}
export function hasRepetitiveDays(timeline=[]){const perDay=new Map(),titles=new Map();for(const item of timeline.filter(x=>x.type==="main-attraction"||x.type==="secondary-experience")){const key=`${item.day}:${item.type}`;perDay.set(key,(perDay.get(key)||0)+1);const title=String(item.title||"").trim().toLowerCase();if(title)titles.set(title,(titles.get(title)||0)+1);}return[...perDay.values()].some(count=>count>1)||[...titles.values()].some(count=>count>=3);}
