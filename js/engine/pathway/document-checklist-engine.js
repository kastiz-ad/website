export function buildDocumentChecklist(requirements=[],metadata={}){return requirements.map(requirement=>{const state=metadata[requirement.id]||{};return{...requirement,userProvided:Boolean(state.provided),expiresAt:state.expiresAt||null,status:state.provided?"PROVIDED":"MISSING",needsAppointment:Boolean(state.needsAppointment),needsVerification:true,dependentSteps:state.dependentSteps||[],contentStored:false};});}
