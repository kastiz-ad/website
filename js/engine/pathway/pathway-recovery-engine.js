const routes={PROVIDER_UNAVAILABLE:["Preserve the pathway and compare a later intake","Prepare a verified alternative provider"],TOO_EXPENSIVE:["Reduce scope while preserving the priority outcome","Use a lower-cost foundation stage first"],ELIGIBILITY_CHANGED:["Pause irreversible commitments","Recheck an alternative legal route"],DEADLINE_MISSED:["Find the next verified intake","Rebuild dependent dates"],DOCUMENT_EXPIRED:["Renew the document first","Keep later steps provisional"]};
export function prepareRecovery(reason="PROVIDER_UNAVAILABLE"){return{state:"NEEDS_RECOVERY",reason,routes:routes[reason]||["Revalidate the affected dependency","Prepare a safe alternative"],preserveUnaffectedMilestones:true,externalExecution:false};}
