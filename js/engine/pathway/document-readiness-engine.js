export function evaluateDocumentReadiness(checklist=[]){const missing=checklist.filter(item=>["MISSING","EXPIRED","NEEDS_RENEWAL"].includes(item.status));return{ready:missing.length===0,missing:missing.map(item=>item.id),expired:missing.filter(item=>item.status==="EXPIRED").map(item=>item.id),needsRenewal:missing.filter(item=>item.renewalRequired).map(item=>item.id),sensitiveContentsStored:false};}
