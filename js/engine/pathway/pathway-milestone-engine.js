import{classifyPathwayAction}from"./action-classification.js";
export function buildMilestones(type){const labels=["Confirm outcome and constraints","Verify eligibility","Prepare required documents","Compare and select providers","Prepare application information","Review approval-required actions","Monitor external confirmations","Confirm completion criteria"];return labels.map((title,index)=>({id:`M${index+1}`,title,classification:classifyPathwayAction(title),status:index?"NOT_STARTED":"PREPARING",dependsOn:index?[`M${index}`]:[],deadline:null,responsible:index===1?"USER_AND_ONE":"ONE"}));}
