# Account and Identity Deletion Policy — Draft

Requires Korean privacy/legal review. Identity Pass deletion requires recent device authentication, affects only the authenticated owner, revokes vault/provider access, marks encrypted records deleted, schedules approved provider deletion requests, and creates a privacy-safe event without plaintext values. Account deletion additionally revokes sessions and removes or justifiably anonymizes user-owned records.
