# Presentation assets

The live screenshots for this folder must be captured from the tested `funding-demo-final` build so that they never fabricate provider or live-data status. Capture at 1440×900 and 390×844 using the routes listed in `INVESTOR_DEMO_SCRIPT.md`. The offline fallback is in `offline-demo/index.html`.
